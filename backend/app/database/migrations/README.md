# Database migrations

Alembic revision files are stored in `versions/`. Phase 2 provides the explicit
`20260829_0001` initial schema revision. It can be applied when `DATABASE_URL`
points to a running PostgreSQL database.
